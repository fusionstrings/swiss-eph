# Build artifacts
target/
lib/wasi/
#lib/wasm-*/     <- Un-commenting to allow publishing? No, they are generated.
#lib/native/     <- Actually I should exclude them but make exceptions?
# Or relying on publish.include to force include them?
# JSR documentation says 'publish.include' overrides .gitignore if used?
# No, 1460 error said 'module ... was excluded ... hint: remove from exclude ... or unexclude from gitignore'
# So I MUST unexclude them in .gitignore or JSR fails.

lib/wasi/
lib/wasm/
lib/wasm-inline/
lib/native/
lib/artifacts/
swetest_enhanced
swetest_enhanced.wasm
bench_native

# Toolchain
toolchain/
wasi-sdk-*.tar.gz
.cargo/

# OS Specific
.DS_Store
Thumbs.db

# Editor Specific
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Deno
.deno/
deno.lock.back

# Node
node_modules/
npm/

# Vendor build artifacts
vendor/swisseph/*.o
vendor/swisseph/libswe.a
vendor/swisseph/swetest

# Environment
.env*

# Legacy (removing, should already be gone)
/generated/
dist/
artifacts/
browser/wrapper.ts
output.txt# Ephemeral vendor copy for cargo publish
crates/swiss-eph/vendor/
